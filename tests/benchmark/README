1. run python script as follows, replacing myscript with a given python script in this directory
python -m cProfile -o myscript.profile myscript.py

Or, alternatively, to run all at once, use 'run_tests.py benchmarks' in the tests directory

2. look at profile using runsnakerun or snakeviz
runsnake myscript.profile
snakeviz myscript.profile
